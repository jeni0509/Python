str = "troll geek coding made fun"
#split() it will space separate by default add comma  and list it as a LIST
#lst= str.split()
#print(lst)
#print(type(lst))
#strip() when we want to remove character or space in the string we use we use rstrip,lstrip
print(str.rstrip())
sub="fun"
print(str.find(sub))
#find() sub string =sub() we can find that particular word in the particular index using this
#if there is no sub string it will throw -1
#ifdigit() it will return true or false
string="123"
print(string.isdigit())
#upper(),lower(),isspace(),isalymn is similar to this