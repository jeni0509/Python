# it is a list of array starts from oth index and it will go
lst=[1,2,3]
for i in lst:
    print(i)
# mutability we can change the value of a individual data after assignment in array,dic,set
#nonmutability we cannot change the value of a individual data after assignment  typil, string,int
print(len(lst))# length of the list
print(max(lst))#max value in list
print(min(lst))
